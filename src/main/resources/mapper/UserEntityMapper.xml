<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="house.rental.system.dao.UserMapper">


    <insert id="insertUser">
        insert into user  (username,password,email,user.code,status)
        values (#{userInfo.username,jdbcType=VARCHAR},#{userInfo.password,jdbcType=VARCHAR},#{userInfo.email,jdbcType=VARCHAR},#{userInfo.code,jdbcType=VARCHAR},#{userInfo.status,jdbcType=INTEGER})
    </insert>
    <select id="check" resultType="java.lang.Integer">
        select id from user where username=#{user.username} and code=#{user.code}
    </select>
    <update id="updateStatus">
        update user set status = 1 where username=#{user.username}
    </update>
</mapper>
